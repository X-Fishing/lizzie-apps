# Configuracao do Supabase (Vite expoe variaveis com prefixo VITE_ ao client).
# A anon key e publica (vai para o browser), mas fica fora do git por organizacao.
# No Netlify, defina estas duas variaveis em Site settings > Environment variables.
VITE_SUPABASE_URL=
VITE_SUPABASE_KEY=
