# Dependencias
node_modules/

# Build do Vite
dist/
dev-dist/

# Variaveis de ambiente (a anon key e publica, mas fica fora do git)
.env
.env.*
!.env.example
# Modelos dos secrets/credenciais (sem valores) — estes podem versionar.
# Os arquivos preenchidos (sem o .example) seguem ignorados pelo .env.* acima.
!supabase/.env.*.example
*.pem
*.key
*.crt
*.p12
*.pfx
credentials.json
token.json
service-account*.json

# Supabase CLI (temporarios locais, nao versionar)
supabase/.temp/

# Config local do MCP (tooling da maquina; usa ${SUPABASE_ACCESS_TOKEN} do ambiente)
.mcp.json

# Sistema operacional
.DS_Store
Thumbs.db

# Clone acidental do proprio repo dentro da pasta
lizzie-apps/

# Netlify CLI local
.netlify/

# Scripts temporarios de teste (Claude) - nunca commitar
.scripts-temp/
